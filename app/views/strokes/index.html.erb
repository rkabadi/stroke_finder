<h1>Listing strokes</h1>

<table>
  <thead>
    <tr>
      <th>Player</th>
      <th>Shot</th>
      <th>Handedness</th>
      <th>Grip</th>
      <th>Spin</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @strokes.each do |stroke| %>
      <tr>
        <td><%= stroke.player_id %></td>
        <td><%= stroke.shot %></td>
        <td><%= stroke.handedness %></td>
        <td><%= stroke.grip %></td>
        <td><%= stroke.spin %></td>
        <td><%= link_to 'Show', stroke %></td>
        <td><%= link_to 'Edit', edit_stroke_path(stroke) %></td>
        <td><%= link_to 'Destroy', stroke, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Stroke', new_stroke_path %>
